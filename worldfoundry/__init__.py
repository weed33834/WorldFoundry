"""Root package for WorldFoundry — a framework for evaluating world-generation models.

The public API is distributed across sub-packages such as
:class:`worldfoundry.evaluation`, :class:`worldfoundry.cli`,
:class:`worldfoundry.core`, and :class:`worldfoundry.pipelines`.
This ``__init__.py`` does not re-export any symbols explicitly;
import directly from the relevant sub-package instead.
"""

from __future__ import annotations
