"""Vendored SCOPE runtime source.

This package contains the upstream SCOPE inference code and its local
DiffSynth fork. Heavy dependencies and model weights are intentionally loaded
only by the subprocess entry point.
"""
