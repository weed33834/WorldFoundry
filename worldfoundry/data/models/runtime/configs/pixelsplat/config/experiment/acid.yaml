# @package _global_

defaults:
  - override /dataset: re10k
  - override /model/encoder: epipolar
  - override /model/encoder/backbone: dino
  - _self_

dataset:
  image_shape: [256, 256]
  roots: [datasets/acid]
