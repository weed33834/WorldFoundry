artifact_filename: rt1_action_trace.json
artifact_kind: action_trace
backend_stage: in_tree_runtime
checkpoints:
- local_dir: ${WORLDFOUNDRY_CKPT_DIR}/embodied_action/rt-1/rt1main
  role: official_repo_savedmodel_rt1main
id: rt-1
input_schema:
  actions:
  - robot_action
  - proprio
  - action_space
  image: true
  instruction: true
  prompt: true
  video: false
integration_status: runtime_ported
notes:
- RT-1/Robotics Transformer is modeled as a language-conditioned robot policy.
- The profile expects an RT-1 TensorFlow SavedModel directory at ${WORLDFOUNDRY_CKPT_DIR}/embodied_action/rt-1/rt1main
  when users stage one locally. The current workspace does not include that SavedModel.
- The profile emits action_trace artifacts; benchmark wrappers are responsible for
  environment replay and success metrics.
- WorldFoundry vendors RT-1 SavedModel runtime and upstream source under worldfoundry/synthesis/action_generation/rt1/rt1_runtime.
  py_compile, import/from_pretrained, and plan generation passed; real predict_action
  remains blocked because the profile checkpoint directory is not staged locally.
runtime_status: in_tree_rt1_runtime_ported_savedmodel_checkpoint_missing
task_family: vla_policy
verification_status: plan_only_ready_checkpoint_required
