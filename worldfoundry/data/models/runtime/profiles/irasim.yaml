artifact_filename: irasim.mp4
artifact_kind: generated_world
backend_stage: in_tree_runtime_plan
id: irasim
input_schema:
  actions:
  - robot_action
  image: true
  prompt: true
integration_status: planned
notes:
- WorldFoundry vendors the IRASim model, diffusion, and inference pipeline modules under worldfoundry/synthesis/visual_generation/irasim/irasim_runtime.
- Upstream training dataloaders, evaluation scripts, and interactive application demos are pruned from the runtime package.
- Published checkpoint archives are not HF model repos staged by the current downloader, so full generation remains gated
  on caller-provided checkpoints and task fixtures.
runtime_status: in_tree_irasim_runtime_ported_checkpoint_pending
