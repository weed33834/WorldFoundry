schema_version: 2
model_id: 4d-gs
display_name: 4D-GS
task_family: four_dimension
groups:
- 4d
- official_repo
source_repos:
- url: https://github.com/hustvl/4DGaussians
input_schema:
  prompt: false
  image: false
  video: true
  actions:
  - dynamic_scene_dataset
  - render_path
artifact_kind: generated_4d_scene
artifact_filename: 4dgs_output.ply
execution:
  backend_stage: metadata_only_training_or_reconstruction_workflow
  runtime_status: blocked_no_infer_only_entrypoint
  integration_status: blocked
  pipeline_binding: 4d-gs
integration:
  status: blocked
notes:
- WorldFoundry records 4D-GS as an upstream 4D reconstruction project, but does not expose it as a release-facing Studio inference job.
- The official workflow requires prepared dynamic-scene data and optimization/training before rendering, so it is hidden from Studio until an infer-only artifact path is implemented.
