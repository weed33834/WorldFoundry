artifact_filename: pi0_action_trace.json
artifact_kind: action_trace
backend_stage: in_tree_runtime
checkpoints:
- local_dir: ${WORLDFOUNDRY_CKPT_DIR}/openpi/openpi-assets/checkpoints/pi0_droid
  path: gs://openpi-assets/checkpoints/pi0_droid
  role: pi0_droid_checkpoint
  status: orbax_checkpoint_metadata_validated
- local_dir: ${WORLDFOUNDRY_CKPT_DIR}/openpi/openpi-assets/checkpoints/pi0_base
  path: gs://openpi-assets/checkpoints/pi0_base
  role: pi0_base_checkpoint
  status: orbax_checkpoint_metadata_validated
execution:
  environment: openpi
id: pi0
input_schema:
  actions:
  - robot_action
  - proprio
  - action_space
  image: true
  instruction: true
  prompt: true
  video: false
integration_status: runtime_ported
notes:
- pi0 is a concrete Physical Intelligence OpenPI algorithm entry routed through the
  in-tree OpenPI runtime.
- WorldFoundry keeps the inference-only OpenPI runtime directly under worldfoundry/synthesis/action_generation/openpi.
- Default runtime config uses the in-tree pi0_droid policy config; both declared
  Orbax checkpoints are locally staged and their metadata trees are validated.
- This profile emits action_trace artifacts and is not a video-generation profile.
runtime_status: in_tree_openpi_pi0_runtime_ported_checkpoint_staged_gpu_parity_pending
task_family: vla_policy
verification_status: checkpoint_parity_pending
