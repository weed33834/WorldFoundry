artifact_filename: pointworld_runtime_plan.json
artifact_kind: generated_3d_asset
backend_stage: worldfoundry_runtime_plan
display_name: PointWorld
id: pointworld
input_schema:
  actions:
  - robot_action
  - point_cloud_condition
  image: true
  point_cloud: true
  prompt: true
  video: true
integration_status: runtime_ported
notes:
- PointWorld official source route is registered; checkpoint/point-cloud assets are
  runtime-gated.
- Missing checkpoints/assets are reported by the runtime plan and are not catalog
  blockers.
- WorldFoundry tracks upstream source and checkpoint metadata only; no runnable pipeline
  is registered.
- Official source is bound from ${WORLDFOUNDRY_MODEL_SOURCE_DIR}/PointWorld at 05484826dfef74cbe278a3974179a5a16705d35d.
- Real point-cloud world prediction remains blocked until nvidia/PointWorld_models
  and official runtime parity are staged.
runtime_status: route_ready_assets_pending
task_family: three_dimension
execution:
  pipeline_binding: pointworld
  backend_stage: worldfoundry_runtime_plan
  runtime_status: route_ready_assets_pending
