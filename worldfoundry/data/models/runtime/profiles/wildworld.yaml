artifact_filename: wildworld_runtime_plan.json
artifact_kind: evaluation_report
backend_stage: worldfoundry_runtime_plan
display_name: WildWorld
id: wildworld
input_schema:
  actions:
  - evaluation_query
  image: true
  prompt: true
  video: true
integration_status: runtime_ported
notes:
- WildWorld official source route is registered; benchmark/dataset assets are runtime-gated.
- Missing checkpoints/assets are reported by the runtime plan and are not catalog
  blockers.
- WorldFoundry tracks upstream source and checkpoint metadata only; no runnable pipeline
  is registered.
- Official source is bound from ${WORLDFOUNDRY_MODEL_SOURCE_DIR}/WildWorld at 23971043ae34d802b83661a964fe291f14d277f7.
- Real benchmark/dataset execution remains blocked until license review, assets, and
  evaluator parity are staged.
runtime_status: route_ready_assets_pending
task_family: world_model
execution:
  pipeline_binding: wildworld
  backend_stage: worldfoundry_runtime_plan
  runtime_status: route_ready_assets_pending
