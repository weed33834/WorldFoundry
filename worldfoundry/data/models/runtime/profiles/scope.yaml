artifact_filename: scope.mp4
artifact_kind: generated_video
backend_stage: in_tree_official_cli
checkpoints:
- local_dir: ${WORLDFOUNDRY_HFD_ROOT}/SCOPE
  repo_id: zizhaotong/SCOPE
  role: unified_scope_dit_t5_vae_tokenizer_checkpoint
display_name: SCOPE
id: scope
input_schema:
  actions:
  - action_parquet
  image: true
  prompt: true
  video: false
integration_status: verified
notes:
- WorldFoundry vendors the official SCOPE inference source and DiffSynth fork under worldfoundry/synthesis/visual_generation/scope/scope_runtime.
- SCOPEPipeline fails fast unless execute=True is supplied, then runs the in-tree
  inference.py subprocess with staged z2tong/SCOPE weights.
runtime_status: in_tree_scope_runtime_checkpoint_pending
task_family: world_model
