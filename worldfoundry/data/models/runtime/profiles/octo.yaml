artifact_filename: octo_action_trace.json
artifact_kind: action_trace
backend_stage: in_tree_runtime
checkpoints:
- local_dir: ${WORLDFOUNDRY_HFD_ROOT}/rail-berkeley--octo-base-1.5
  repo_id: rail-berkeley/octo-base-1.5
  role: base_policy_checkpoint
- local_dir: ${WORLDFOUNDRY_CKPT_DIR}/octo-small-1.5
  repo_id: rail-berkeley/octo-small-1.5
  role: small_policy_checkpoint
id: octo
input_schema:
  actions:
  - robot_action
  - proprio
  - action_space
  image: true
  instruction: true
  prompt: true
  video: true
integration_status: integrated
notes:
- Octo is a generalist robot policy profile. It is evaluated through embodied action
  rollouts, not video generation.
- Both octo-base-1.5 and octo-small-1.5 HF checkpoints are staged for variant-specific
  demos.
- Octo checkpoints are variant-specific; real execution should remain benchmark and
  robot-action-space gated.
runtime_status: in_tree_octo_small_jax_gpu_sample_actions_ready
task_family: vla_policy
verification_status: official_demo_jax_gpu_ready
