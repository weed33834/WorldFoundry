artifact_filename: droid_w_runtime_plan.json
artifact_kind: generated_video
backend_stage: worldfoundry_runtime_plan
display_name: DROID-W
id: droid-w
input_schema:
  actions:
  - robot_action
  - droid_action
  image: true
  prompt: true
  video: true
integration_status: runtime_ported
notes:
- DROID-W official source route is registered; checkpoints/data are runtime-gated.
- Missing checkpoints/assets are reported by the runtime plan and are not catalog
  blockers.
- WorldFoundry tracks upstream source and checkpoint metadata only; no runnable pipeline
  is registered.
- Official source is bound from ${WORLDFOUNDRY_MODEL_SOURCE_DIR}/DROID-W at c3414af6047d06bafc1a6645d09f23247f3b2cdc.
- Real generation/evaluation remains blocked until DROID-W checkpoints, datasets,
  dependencies, and parity records are staged.
runtime_status: route_ready_assets_pending
task_family: robot_world_model
execution:
  pipeline_binding: droid-w
  backend_stage: worldfoundry_runtime_plan
  runtime_status: route_ready_assets_pending
