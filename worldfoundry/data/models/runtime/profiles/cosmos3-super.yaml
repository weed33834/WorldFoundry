schema_version: 2
model_id: cosmos3-super
display_name: Cosmos 3 Super
task_family: world_model
groups:
- world
- video
source_repos:
- url: https://github.com/NVIDIA/cosmos
checkpoints:
- repo_id: nvidia/Cosmos3-Super
  revision: e0262be9d8f7586bc24c069a2aed2b665bdff266
  role: cosmos3_checkpoint
input_schema:
  prompt: true
  image: true
  video: true
  actions:
  - policy
  - forward_dynamics
  - inverse_dynamics
artifact_kind: generated_world
artifact_filename: cosmos3-super.mp4
execution:
  backend_stage: in_tree_diffusers_inference
  checkpoint_revision_policy: exact_sha_for_verified_runs
  load_sound_tokenizer: true
  runtime_status: exact_revision_four_gpu_workspace_t2i_smoke_passed
  integration_status: integrated
  pipeline_binding: cosmos3-super
  environment: cosmos3
integration:
  status: integrated
notes:
- The in-tree generator supports optional synchronized sound and structured policy,
  forward-dynamics, and inverse-dynamics action inference. Reasoner, training, and serving
  workflows are not part of this infer-only profile.
- The exact Super revision completed a real 256x256 two-step Workspace T2I smoke on four
  A100-SXM4-80GB GPUs with the safe decoder-layer-only balanced map. This validates execution,
  not official-resolution or output-quality parity.
- Verified runs must use revision e0262be9d8f7586bc24c069a2aed2b665bdff266 explicitly; directory
  presence without matching .hfd metadata or an exact HF snapshot is not current-revision evidence.
- The shared default safety runtime passed offline GPU construction and a Nano Workspace run with
  exact Guardrail/Qwen3Guard revisions. A combined Super-plus-safety Workspace run is not recorded;
  the gated NVIDIA repository still requires authorization on a fresh machine.
- Super synchronized-sound and action-task Workspace evidence remains pending.
