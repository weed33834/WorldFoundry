schema_version: 2
binding_id: giga-world-policy
model_id: giga-world-policy
runner: worldfoundry.pipeline
pipeline:
  target: worldfoundry.pipelines.component_pipelines:GigaWorldPolicyPipeline
  loading:
    method: from_pretrained
    signature: unified
  invocation:
    mode: native_or_standard_invocation
