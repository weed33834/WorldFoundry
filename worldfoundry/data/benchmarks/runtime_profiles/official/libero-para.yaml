benchmark_ids:
- libero-para
blocked_reason: LIBERO-Para official-result normalizer is available; full status still needs the upstream checkout, 
  dataset, policy checkpoint, and official rollout/result dump.
environment_id: giga-world-policy-py311
id: libero-para
name: LIBERO-Para
needs_new_env: false
notes:
- LIBERO-Para is tracked as an embodied benchmark, not a model runner; WorldFoundry normalizes official-shaped result 
  exports only.
- Full official status requires the cau-hai-lab/LIBERO-Para runtime, HAI-Lab/LIBERO-Para data, policy checkpoint, and 
  paraphrase split mapping.
preflight_command: PYTHONPATH=${WORLDFOUNDRY_CACHE_DIR}/repos/cau-hai-lab--LIBERO-Para:src 
  ${WORLDFOUNDRY_CONDA_ENVS_ROOT}/worldfoundry-giga-world-policy-py311/bin/python -m 
  worldfoundry.evaluation.tasks.execution.orchestration.runtime_preflight --profile libero-para --manifest 
  worldfoundry/data/benchmarks/runtime_profiles/official/libero-para.yaml --output-dir 
  tmp/worldfoundry_preflight/libero-para --json
python_path: ${WORLDFOUNDRY_CONDA_ENVS_ROOT}/worldfoundry-giga-world-policy-py311/bin/python
pythonpath_roots:
- ${WORLDFOUNDRY_CACHE_DIR}/repos/cau-hai-lab--LIBERO-Para
required_assets:
  local_dataset_paths:
  - ${WORLDFOUNDRY_HFD_DATASET_ROOT}/HAI-Lab__LIBERO-Para
  local_repo_paths:
  - ${WORLDFOUNDRY_CACHE_DIR}/repos/cau-hai-lab--LIBERO-Para
  task_yaml: worldfoundry/data/benchmarks/tasks/external/libero-para.yaml
required_env:
- WORLDFOUNDRY_LIBERO_PARA_ROOT
- WORLDFOUNDRY_LIBERO_PARA_RESULTS_PATH
- WORLDFOUNDRY_LIBERO_PARA_DATASET_ROOT
required_packages:
- numpy
- LIBERO-Para checkout
required_paths:
- id: official_result_normalizer
  path: worldfoundry/evaluation/tasks/embodied/normalizer.py
- id: official_repo_1
  path: ${WORLDFOUNDRY_CACHE_DIR}/repos/cau-hai-lab--LIBERO-Para
- id: local_dataset_1
  path: ${WORLDFOUNDRY_HFD_DATASET_ROOT}/HAI-Lab__LIBERO-Para
- id: task_yaml
  path: worldfoundry/data/benchmarks/tasks/external/libero-para.yaml
requires_cuda_visibility: false
status: official_result_normalizer_ready_full_runtime_pending
optional_imports:
- numpy
base_model_dependencies:
- libero_para_source_assets
- libero_para_dataset_assets
optional_base_model_dependencies:
- spatial_perception_core_stack
validation_command:
- worldfoundry-eval
- zoo
- benchmark-run
- --benchmark-id
- libero-para
- --mode
- official-validation
- --official-results-path
- <official_results.json>
- --output-dir
- <out>
- --json
