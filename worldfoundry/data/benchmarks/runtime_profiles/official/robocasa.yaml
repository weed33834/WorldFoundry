benchmark_ids:
- robocasa
blocked_reason: RoboCasa/robosuite import 已在 headless venv 可见；完整官方状态仍需要 assets、task suite 和 policy rollout。
environment_id: robocasa-headless-venv
id: robocasa
name: RoboCasa
docker:
  image: ghcr.io/allenai/vla-evaluation-harness/robocasa:latest
  source_image: ghcr.io/allenai/vla-evaluation-harness/robocasa:latest
  python_env: robocasa
  gpus: all
needs_new_env: false
notes:
- RoboCasa/robosuite import 已在 headless venv 可见；完整官方状态仍需要 assets、task suite 和 policy rollout。
preflight_command: PYTHONPATH=${WORLDFOUNDRY_CACHE_DIR}/repos/robocasa--robocasa:src 
  ${WORLDFOUNDRY_CONDA_ENVS_ROOT}/worldfoundry-robocasa-headless-venv/bin/python -m 
  worldfoundry.evaluation.tasks.execution.orchestration.runtime_preflight --profile robocasa --manifest 
  worldfoundry/data/benchmarks/runtime_profiles/official/robocasa.yaml --output-dir tmp/worldfoundry_preflight/robocasa 
  --json
python_path: ${WORLDFOUNDRY_CONDA_ENVS_ROOT}/worldfoundry-robocasa-headless-venv/bin/python
pythonpath_roots:
- ${WORLDFOUNDRY_CACHE_DIR}/repos/robocasa--robocasa
required_assets:
  local_repo_paths:
  - ${WORLDFOUNDRY_CACHE_DIR}/repos/robocasa--robocasa
  task_yaml: worldfoundry/data/benchmarks/tasks/external/robocasa.yaml
required_env:
- WORLDFOUNDRY_ROBOCASA_ROOT
- WORLDFOUNDRY_ROBOCASA_RESULTS_PATH
required_packages:
- numpy
- RoboCasa checkout
required_paths:
- id: official_result_normalizer
  path: worldfoundry/evaluation/tasks/embodied/normalizer.py
- id: official_repo_1
  path: ${WORLDFOUNDRY_CACHE_DIR}/repos/robocasa--robocasa
- id: task_yaml
  path: worldfoundry/data/benchmarks/tasks/external/robocasa.yaml
requires_cuda_visibility: false
status: official_result_normalizer_ready_full_runtime_pending
optional_imports:
- numpy
optional_base_model_dependencies:
- spatial_perception_core_stack
validation_command:
- worldfoundry-eval
- zoo
- benchmark-run
- --benchmark-id
- robocasa
- --mode
- official-validation
- --official-results-path
- <official_results.json>
- --output-dir
- <out>
- --json
