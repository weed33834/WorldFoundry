benchmark_ids:
- devil-dynamics
blocked_reason: DEVIL Dynamics official-result normalization is wired; complete generated artifacts and 
  leaderboard parity remain pending.
environment_id: worldplay-devil-dynamics-contract
id: devil-dynamics
name: DEVIL Dynamics
needs_new_env: false
notes:
- WorldFoundry runs an in-tree DEVIL artifact-score runtime and also normalizes caller-provided official result files.
- Full validation requires generated artifacts for the official prompt/task suite plus scorer or judge assets.
preflight_command: PYTHONPATH=. ${WORLDFOUNDRY_UNIFIED_PYTHON:-python} -m
  worldfoundry.evaluation.tasks.execution.orchestration.runtime_preflight --profile devil-dynamics --manifest
  worldfoundry/data/benchmarks/runtime_profiles/official/devil-dynamics.yaml --output-dir 
  tmp/worldfoundry_preflight/devil-dynamics --json
python_path: ${WORLDFOUNDRY_UNIFIED_PYTHON:-python}
pythonpath_roots:
- .
required_assets:
  task_yaml: worldfoundry/data/benchmarks/tasks/external/devil-dynamics.yaml
required_env:
- WORLDFOUNDRY_GENERATED_ARTIFACT_DIR
optional_env:
- WORLDFOUNDRY_DEVIL_DYNAMICS_ARTIFACT_SCORE_DIR
- WORLDFOUNDRY_DEVIL_DYNAMICS_RESULTS_PATH
required_imports:
- numpy
required_packages:
- numpy
- id: official_runner_script
  path: worldfoundry/evaluation/tasks/execution/runners/devil_dynamics/run_devil_dynamics_official_runner.py
required_paths:
- id: task_yaml
  path: worldfoundry/data/benchmarks/tasks/external/devil-dynamics.yaml
requires_cuda_visibility: false
status: in_tree_official_runtime_ready_full_metric_assets_pending
optional_imports:
- numpy
optional_base_model_dependencies:
- video_world_perception_stack
validation_command:
- worldfoundry-eval
- zoo
- benchmark-run
- --benchmark-id
- devil-dynamics
- --mode
- official-validation
- --official-results-path
- <official_results.json>
- --generated-artifact-dir
- <generated_videos>
- --output-dir
- <out>
- --json
