benchmark_ids:
- calvin
blocked_reason: CALVIN 官方 rollout 需要数据集 split、policy checkpoint 和 long-horizon evaluator 输出；轻量 preflight 不启动仿真/评测。
environment_id: giga-world-policy-py311
id: calvin
name: CALVIN
docker:
  image: ghcr.io/allenai/vla-evaluation-harness/calvin:latest
  source_image: ghcr.io/allenai/vla-evaluation-harness/calvin:latest
  python_env: calvin
  gpus: all
  env:
  - NVIDIA_DRIVER_CAPABILITIES=all
needs_new_env: false
notes:
- CALVIN 官方 rollout 需要数据集 split、policy checkpoint 和 long-horizon evaluator 输出；轻量 preflight 不启动仿真/评测。
preflight_command: PYTHONPATH=${WORLDFOUNDRY_CACHE_DIR}/repos/mees--calvin:src 
  ${WORLDFOUNDRY_CONDA_ENVS_ROOT}/worldfoundry-giga-world-policy-py311/bin/python -m 
  worldfoundry.evaluation.tasks.execution.orchestration.runtime_preflight --profile calvin --manifest 
  worldfoundry/data/benchmarks/runtime_profiles/official/calvin.yaml --output-dir tmp/worldfoundry_preflight/calvin 
  --json
python_path: ${WORLDFOUNDRY_CONDA_ENVS_ROOT}/worldfoundry-giga-world-policy-py311/bin/python
pythonpath_roots:
- ${WORLDFOUNDRY_CACHE_DIR}/repos/mees--calvin
required_assets:
  local_repo_paths:
  - ${WORLDFOUNDRY_CACHE_DIR}/repos/mees--calvin
  task_yaml: worldfoundry/data/benchmarks/tasks/external/calvin.yaml
required_env:
- WORLDFOUNDRY_CALVIN_ROOT
- WORLDFOUNDRY_CALVIN_DATASET_ROOT
- WORLDFOUNDRY_CALVIN_RESULTS_PATH
required_packages:
- numpy
required_paths:
- id: official_result_normalizer
  path: worldfoundry/evaluation/tasks/embodied/normalizer.py
- id: official_repo_1
  path: ${WORLDFOUNDRY_CACHE_DIR}/repos/mees--calvin
- id: task_yaml
  path: worldfoundry/data/benchmarks/tasks/external/calvin.yaml
requires_cuda_visibility: false
status: official_result_normalizer_ready_full_runtime_pending
optional_imports:
- numpy
optional_base_model_dependencies:
- spatial_perception_core_stack
validation_command:
- worldfoundry-eval
- zoo
- benchmark-run
- --benchmark-id
- calvin
- --mode
- official-validation
- --official-results-path
- <official_results.json>
- --output-dir
- <out>
- --json
