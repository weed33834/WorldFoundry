benchmark_ids:
- bridgedata-v2
blocked_reason: BridgeData V2 需要已物化的评测 split 和 policy-result import；当前只配置通用 Python 环境。
environment_id: worldplay-core
id: bridgedata-v2
name: BridgeData V2
needs_new_env: false
notes:
- BridgeData V2 需要已物化的评测 split 和 policy-result import；当前只配置通用 Python 环境。
preflight_command: PYTHONPATH=${WORLDFOUNDRY_CACHE_DIR}/repos/rail-berkeley--bridge_data_v2:src 
  ${WORLDFOUNDRY_CONDA_ROOT}/worldplay/bin/python -m 
  worldfoundry.evaluation.tasks.execution.orchestration.runtime_preflight --profile bridgedata-v2 --manifest 
  worldfoundry/data/benchmarks/runtime_profiles/official/bridgedata-v2.yaml --output-dir 
  tmp/worldfoundry_preflight/bridgedata-v2 --json
python_path: ${WORLDFOUNDRY_CONDA_ROOT}/worldplay/bin/python
pythonpath_roots:
- ${WORLDFOUNDRY_CACHE_DIR}/repos/rail-berkeley--bridge_data_v2
required_assets:
  local_dataset_paths:
  - ${WORLDFOUNDRY_DATA_DIR}/datasets/bridgedata-v2
  local_repo_paths:
  - ${WORLDFOUNDRY_CACHE_DIR}/repos/rail-berkeley--bridge_data_v2
  task_yaml: worldfoundry/data/benchmarks/tasks/external/bridgedata-v2.yaml
required_env:
- WORLDFOUNDRY_BRIDGEDATA_V2_DATASET_ROOT
- WORLDFOUNDRY_BRIDGEDATA_V2_RESULTS_PATH
required_packages:
- numpy
required_paths:
- id: official_result_normalizer
  path: worldfoundry/evaluation/tasks/embodied/normalizer.py
- id: task_yaml
  path: worldfoundry/data/benchmarks/tasks/external/bridgedata-v2.yaml
requires_cuda_visibility: false
status: official_result_normalizer_ready_full_runtime_pending
optional_imports:
- numpy
optional_base_model_dependencies:
- spatial_perception_core_stack
validation_command:
- worldfoundry-eval
- zoo
- benchmark-run
- --benchmark-id
- bridgedata-v2
- --mode
- official-validation
- --official-results-path
- <official_results.json>
- --output-dir
- <out>
- --json
