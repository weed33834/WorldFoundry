benchmark_ids:
- maniskill
blocked_reason: ManiSkill/SAPIEN 可见；完整官方状态仍需要任务套件、assets 和 policy rollout，不在本轮运行。
environment_id: maniskill
id: maniskill
name: ManiSkill
needs_new_env: false
notes:
- ManiSkill/SAPIEN 可见；完整官方状态仍需要任务套件、assets 和 policy rollout，不在本轮运行。
preflight_command: PYTHONPATH=${WORLDFOUNDRY_CACHE_DIR}/repos/haosulab--ManiSkill:src python -m
  worldfoundry.evaluation.tasks.execution.orchestration.runtime_preflight --profile maniskill --manifest
  worldfoundry/data/benchmarks/runtime_profiles/official/maniskill.yaml --output-dir
  tmp/worldfoundry_preflight/maniskill --json
python_path: python
pythonpath_roots:
- ${WORLDFOUNDRY_CACHE_DIR}/repos/haosulab--ManiSkill
required_assets:
  local_repo_paths:
  - ${WORLDFOUNDRY_CACHE_DIR}/repos/haosulab--ManiSkill
  task_yaml: worldfoundry/data/benchmarks/tasks/external/maniskill.yaml
required_env:
- WORLDFOUNDRY_MANISKILL_ROOT
- WORLDFOUNDRY_MANISKILL_RESULTS_PATH
required_packages:
- numpy
- sapien
- mani_skill
required_paths:
- id: official_result_normalizer
  path: worldfoundry/evaluation/tasks/embodied/normalizer.py
- id: official_repo_1
  path: ${WORLDFOUNDRY_CACHE_DIR}/repos/haosulab--ManiSkill
- id: task_yaml
  path: worldfoundry/data/benchmarks/tasks/external/maniskill.yaml
requires_cuda_visibility: true
status: official_result_normalizer_ready_full_runtime_pending
optional_imports:
- numpy
optional_base_model_dependencies:
- spatial_perception_core_stack
validation_command:
- worldfoundry-eval
- zoo
- benchmark-run
- --benchmark-id
- maniskill
- --mode
- official-validation
- --official-results-path
- <official_results.json>
- --output-dir
- <out>
- --json
