"""Module for base_models -> diffusion_model -> video -> cosmos -> cosmos2 -> runtime -> cosmos_predict2 -> cosmos_predict2 -> _src -> imaginaire -> inference_config.py functionality."""

class ImaginaireTrainer:
    """Inference-only placeholder kept so Cosmos2 config modules can import."""

    def __init__(self, *args, **kwargs):
        """Init."""
        raise RuntimeError("Cosmos2 training is not vendored in WorldFoundry base_models.")
